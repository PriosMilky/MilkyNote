{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Izin akses file MilkyNote",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fs:default",
    {
      "identifier": "fs:scope",
      "allow": [
        "$HOME/Documents/Note",
        "$HOME/Documents/Note/*",
        "$HOME/Documents/Note/**"
      ]
    }
  ]
}